package com.google.firebase.appindexing.internal;

import android.util.Log;
import com.google.firebase.appindexing.FirebaseAppIndex;

public final class zzw {
    public static int zzgo(String str) {
        if (Log.isLoggable(FirebaseAppIndex.APP_INDEXING_API_TAG, 3) ? true : Log.isLoggable(FirebaseAppIndex.APP_INDEXING_API_TAG, 3)) {
            return Log.d(FirebaseAppIndex.APP_INDEXING_API_TAG, str);
        }
        return 0;
    }
}
